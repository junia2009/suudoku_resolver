# ナンプレ絶対解くマン — Sudoku Solver (ver 1.5.0)

カメラ撮影・ギャラリー読み込みから数独を自動解読するウェブアプリ（PWA）です。  
GitHub Pages で動作し、インストール不要でスマートフォン・PC から利用できます。

## 🔗 公開URL
`https://<あなたのGitHubユーザー名>.github.io/<リポジトリ名>/`

---

## 機能

| ステップ | 処理内容 |
|----------|----------|
| 1. 画像入力 | カメラ撮影 または ギャラリーから画像選択 |
| 2. 歪み補正 | OpenCV.js で数独グリッドを自動検出・射影変換。検出失敗時は4隅を手動タップで指定可能 |
| 3. 数字認識 | Tesseract.js OCR (デュアルPSM + 複数前処理バリエーション) で 1〜9 を認識。誤認識はタップして修正可能 |
| 4. 解答表示 | 制約伝播 + バックトラッキング (MRV) で解を求めて表示 |

---

## 技術スタック

- **フロントエンド**: HTML / CSS / Vanilla JavaScript (モジュール構成) / PWA (Service Worker)
- **画像処理**: [OpenCV.js 4.8.0](https://docs.opencv.org/4.8.0/opencv.js) — CLAHE・ガウシアンブラー・適応的閾値・形態学的処理・輪郭検出・射影変換・Hough 線検出
- **数字認識**: [Tesseract.js 5](https://tesseract.projectnaptha.com/) — デュアル PSM OCR エンジン
- **ソルバー**: 制約伝播 (Naked Singles + Hidden Singles) + バックトラッキング (MRV ヒューリスティック)
- **ホスティング**: GitHub Pages (静的サイト)

---

## 数字認識ロジックの詳細

数字認識は `js/digitRecognizer.js` (v1.4.0) が担当します。  
Tesseract.js を使ったデュアル PSM アプローチで、各セルを多数決方式で判定します。

### 1. OCR ワーカーの初期化

起動時に 2 つの Tesseract.js ワーカーを並列で初期化します。

| ワーカー | PSM | 概要 |
|----------|-----|------|
| `_worker10` | PSM 10 — Single character | 1 文字だけを含む画像として認識 |
| `_worker13` | PSM 13 — Raw line | ページセグメンテーションなしで raw テキストとして認識 |

どちらも `tessedit_char_whitelist: '123456789'` を設定し、数字以外の誤検出を防ぎます。

### 2. 空セル判定 (`_isEmpty`)

OCR 前に各セル画像が「空白（数字なし）」かどうかを判定し、空白セルは OCR をスキップします。

1. セル中央部（上下左右 20% マージンを除いた領域）のピクセル輝度を集計します。
2. **標準偏差 < 12** → 輝度が均一 → 空白と判定
3. **暗いピクセルの割合 < 1%**（閾値 = 平均輝度 × 0.6 未満のピクセル）→ 数字なし → 空白と判定

上記どちらかに該当するセルは `0`（空白）として扱い、OCR をスキップします。

### 3. 前処理バリエーションの生成

空白でないセルには以下の前処理を施した複数のバリエーション画像を用意し、それぞれ OCR を実行します。

| バリエーション | 処理内容 |
|----------------|----------|
| グレー × 3倍拡大 | パディング付きで 3 倍に拡大 |
| グレー × 4倍拡大 | パディング付きで 4 倍に拡大 |
| コントラスト強調 × 3倍 | ストレッチング (min-max 正規化) 後に 3 倍拡大 |
| コントラスト強調 × 4倍 | ストレッチング後に 4 倍拡大 |
| 数字中心切り出し (グレー) | バウンディングボックスで数字を切り出し・正方形にして 3 倍拡大 |
| 数字中心切り出し (コントラスト) | 上記にコントラスト強調を追加 |
| 二値化 × 3倍 | 輝度 128 で閾値処理し黒白反転、3 倍拡大 |
| 二値化 × 4倍 | 同上、4 倍拡大 |

**数字中心切り出し (`_centerDigit`)**: 平均輝度の 70% 以下の暗いピクセルのバウンディングボックスを求め、正方形にトリミング → 1.4 倍の余白を加えた正方形 ImageData に中央配置します。幅・高さが 4px 未満の場合はスキップ。

**コントラスト強調 (`_enhanceImageData`)**: 全ピクセルの最小値・最大値を取得し、`(値 - min) / (max - min) × 255` でストレッチングします。

### 4. デュアル PSM での OCR 実行と投票

| ワーカー | 対象バリエーション |
|----------|-------------------|
| PSM 10   | 上記の全バリエーション (最大 8 枚) |
| PSM 13   | グレー 3x・4x、コントラスト 3x、数字中心 3x の 4 枚 |

各 OCR 結果は `recognize()` API で取得し、正規表現 `/^[1-9]$/` に一致した場合のみ有効票とします。  
各数字について **得票数** と **最高信頼度スコア** を集計します。

### 5. 最終判定 (多数決 + 信頼度)

```
得票数が最多の数字 = best
```

以下の条件を満たす場合のみセルに数字を設定します（それ以外は `0`）：

- **best の得票数 ≥ 2**、または
- **best の信頼度スコア ≥ 65**

### 6. 数独制約による後処理 (`_resolveConflicts`)

OCR 完了後、認識結果が数独のルールに違反していないかチェックします。

- 同じ行・列・3×3 ブロック内に同じ数字が重複している場合、**後者を `0`（空白）に変更**します。
- 矛盾がなくなるまでこれを繰り返します。

---

## 画像処理ロジックの詳細

画像処理は `js/imageProcessor.js` が担当します。

### グリッド自動検出フロー

1. **グレースケール変換** (`cv.cvtColor`)
2. **コントラスト正規化**: CLAHE (clipLimit=2.0, tileSize=8×8) を優先、失敗時は `equalizeHist` にフォールバック
3. **ガウシアンブラー** (9×9 カーネル)
4. **マルチ閾値戦略**: 以下の 4 パラメータセットで適応的閾値処理を試行し、最初に四辺形が検出できた時点で停止

   | blockSize | C |
   |-----------|---|
   | 11 | 2 |
   | 15 | 3 |
   | 21 | 5 |
   | 7  | 2 |

   各試行で `ADAPTIVE_THRESH_GAUSSIAN_C + THRESH_BINARY_INV` を適用し、3×3 矩形カーネルで `MORPH_CLOSE` を実行。
5. **輪郭検出** (`cv.findContours`): 画像面積の 10% 以上の面積を持つ輪郭のうち、`approxPolyDP` で頂点数が 4 の最大面積の多角形をグリッドとして選択
6. **コーナー整列**: sum (x+y) と diff (x-y) を使って [左上, 右上, 右下, 左下] の順に並べ替え
7. **射影変換** (`cv.getPerspectiveTransform + warpPerspective`): 540×540 px の正方形に変換
8. **セル分割**: Hough 線検出でグリッド線を精密に検出してセル境界を決定（失敗時は均等 60px 分割にフォールバック）。各セルをグレースケール・二値化の 2 種類で切り出し

自動検出が失敗した場合は、ユーザーが画像上の 4 隅を手動タップで指定できます。

---

## ソルバーロジックの詳細

ソルバーは `js/sudokuSolver.js` が担当します。

### 制約伝播 (`_propagate`)

バックトラッキング前に以下 2 つのテクニックで確定できるセルを先に埋めます。

- **Naked Singles**: 候補数字が 1 つしかないセルを即確定
- **Hidden Singles**: 行・列・3×3 ブロック内で特定の数字が 1 箇所にしか入れない場合に確定

これを繰り返し適用して探索空間を大幅に縮小します。

### バックトラッキング + MRV ヒューリスティック (`_backtrack`)

空白セルを選ぶ際に **MRV (Minimum Remaining Values)**: 候補数が最小のセルを優先的に選択します。  
候補数が 0 のセルが存在する場合は即座に矛盾と判定しバックトラックします。

---

## ローカルで動かす

```bash
# Python がある場合
python -m http.server 8080
# または Node.js がある場合
npx serve .
```

ブラウザで `http://localhost:8080` を開いてください。  
**※ `file://` プロトコルでは OpenCV.js / Tesseract.js が動作しません。必ずローカルサーバー経由で開いてください。**

---

## GitHub Pages へのデプロイ

1. このリポジトリを GitHub にプッシュします。
2. GitHub のリポジトリページ → **Settings** → **Pages**
3. **Source** を `Deploy from a branch` に設定し、ブランチ `main` / `/ (root)` を選択して **Save**。
4. 数分後に公開 URL が表示されます。

---

## ファイル構成

```
.
├── index.html              # メインページ (PWA設定含む)
├── manifest.json           # PWA マニフェスト
├── sw.js                   # Service Worker (オフラインキャッシュ)
├── css/
│   └── style.css           # スタイルシート
├── js/
│   ├── app.js              # エントリポイント・状態管理・ステップ制御
│   ├── camera.js           # カメラ/ギャラリー入力
│   ├── imageProcessor.js   # 歪み補正 (OpenCV.js) · セル切り出し
│   ├── digitRecognizer.js  # 数字認識 (Tesseract.js OCR · デュアルPSM)
│   ├── sudokuSolver.js     # 制約伝播 + バックトラッキングソルバー
│   └── ui.js               # UI制御・グリッド描画・手動編集
├── icons/
│   └── icon-192.png        # PWA アイコン
└── README.md
```

---

## 注意事項

- **HTTPS 必須**: カメラ API (`getUserMedia`) は HTTPS 環境のみで使用可能です。GitHub Pages は自動的に HTTPS で提供されます。
- **Tesseract.js の初回ロード**: 初回実行時に言語データファイルをダウンロードします。ネットワーク環境によっては数秒かかる場合があります。
- **グリッド検出精度**: 明るい場所での撮影、グリッドが画面の大部分を占めるように撮影すると精度が上がります。
- **ブラウザ対応**: Chrome / Safari (iOS 14.5+) / Firefox 最新版を推奨します。

---

## ライセンス

MIT License
